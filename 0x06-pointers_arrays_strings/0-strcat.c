#include <string.h>
#include "main.h"

/**
 * _strcat - function that concatenates two string
 * @src: second string
 * @dest: first string
 *
 * Return: resulting string
 */

char *_strcat(char *dest, char *src)
{
	char *dest = s1;
	char *src = s2;
	char *ptr;

	ptr = _strcat(s1, s2);

return (*ptr);
}

